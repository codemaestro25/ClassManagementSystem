from tkinter import *

global createCanvas

mButtCol = "#80edaf"
mFontStyle = "Segoe Print"
mFontSize = 14



# def createCanvas(frame):
    # canvas = Canvas(frame, width=300, height=600, bg='#3e1199')
    # canvas.place(x=0, y=0)
    #
    # Button(canvas, text="Enroll New Student", font=(mFontStyle, mFontSize), borderwidth=0, bg="#80edaf").place(x=60,y=120)
    # Button(canvas, text="Fees", font=(mFontStyle, mFontSize), borderwidth=0, bg="#80edaf").place(x=60, y=220)
    # Button(canvas, text="Students List", font=(mFontStyle, mFontSize), borderwidth=0, bg="#80edaf").place(x=60, y=320)
    # Button(canvas, text="Schedule", font=(mFontStyle, mFontSize), borderwidth=0, bg="#80edaf").place(x=60, y=420)
    # return canvas